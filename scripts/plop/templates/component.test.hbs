import { fixture, html, expect } from '@open-wc/testing';
import {{ tagToDsTitle tag }} from "./{{ tag }}";
import "./{{ tag }}";

describe('{{ tag }}', () => {
  it('renders', async () => {
    const el = await fixture<Ds{{ tagToEpTitle tag }}>(html`<{{ tag }}></{{ tag }}>`);

    expect(el).to.exist;
  });

  it("is accessible", async () => {
    const el = await fixture<{{ tagToDsTitle tag }}>(html`<{{ tag }}></{{ tag }}>`);

    await expect(el).shadowDom.to.be.accessible();
  });
});
